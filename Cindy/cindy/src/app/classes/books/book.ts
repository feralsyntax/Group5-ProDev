export class Book {
  constructor(
    public author: string,
    public title: string,
    public publisher: string
  ) {
    this.author = author;
    this.title = title;
    this.publisher = publisher;
  }
}
